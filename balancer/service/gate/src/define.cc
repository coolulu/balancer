#include "define.h"

const std::string& Define::service_version = "0.1.0";

unsigned short Define::service_id = 0;	// 赋值区域性service_id,赋值于proc.local_service_id
const std::string& Define::service_name = "gate";

const std::string& Define::conf_file_postfix = ".conf";
